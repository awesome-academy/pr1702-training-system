<%= form_for @subject, url: {action: :update}  do |f| %>
  <%= f.hidden_field :subject_id, value: @subject.id %>
  <%= f.hidden_field :trainee_id, value: current_user.id %>
  <%= f.submit t("views.subject.complete_form.complete"), class: "btn btn-primary" %>
<% end %>
